const nextConfig = {
    reactStrictMode: true,
    images: {
        unoptimized: true,
    },
    // Disable the development indicator ('N' icon)
    devIndicators: {
        buildActivity: false,
        appIsrStatus: false,
    },
};

export default nextConfig;
